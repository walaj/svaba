#include "Suffix.h"

void SuffArray::sacaInducedCopying() 
{

  // In the multiple strings case, we need a 2D bit array
  // to hold the L/S types for the suffixes
  /*  char** type_array = new char*[m_num_reads];

  for (size_t i = 0; i < m_num_reads; ++i) 
    {
      size_t s_len = r_length(m_reads[i]) + 1;
      
    }
  */
}
